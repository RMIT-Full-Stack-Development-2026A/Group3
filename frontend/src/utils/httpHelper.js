/**
 * HTTP Client Utility
 * Provides axios instance with interceptors.
 */
const httpClient = {
    get: (url, config) => {},
    post: (url, data, config) => {},
    put: (url, data, config) => {},
    delete: (url, config) => {}
};

export default httpClient;
